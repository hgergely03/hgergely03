---
export interface Props {
    label: string;
    backgroundImageSrc: string;
    class?: string | string[];
    href?: string;
}

const {
    label: firstLineText,
    backgroundImageSrc,
    class: className,
    href,
    ...rest
} = Astro.props;
---

<style define:vars={{ backgroundImage: `url("${backgroundImageSrc}")` }}>
    .project-card {
        background:
            linear-gradient(
                180deg,
                rgba(0, 0, 0, 0.2) 0%,
                rgba(0, 0, 0, 0.8) 100%
            ),
            var(--backgroundImage) #353535 50% / cover no-repeat;
        border-radius: var(--spacing-small);
        box-shadow: 0 0 7px 2px rgba(255, 255, 255, 0.02);
        display: flex;
        flex-direction: column;
        justify-content: end;
        align-items: start;
    }

    .padding {
        padding: var(--spacing-normal) var(--spacing-normal);
    }

    h3 {
        margin: 0;
        font-size: 1.1rem;
        font-weight: 400;
        color: rgba(255, 255, 255, 0.8);
    }

    a {
        text-decoration: none;
        display: flex;
        flex-direction: column;
        justify-content: end;
        height: 100%;
        width: 100%;
        box-sizing: border-box;
    }
</style>

{
    href ? (
        <div class={`project-card ${className || ""}`} {...rest}>
            <a href={href} class="padding">
                <h3>{firstLineText}</h3>
            </a>
        </div>
    ) : (
        <div class={`project-card padding ${className || ""}`} {...rest}>
            <h3>{firstLineText}</h3>
        </div>
    )
}
